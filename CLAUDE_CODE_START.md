# Golem — Claude Code Session Start

Read CLAUDE.md at the repo root. That's your operating context.

If this is the first session, start by:
1. Reading CLAUDE.md fully
2. Scanning docs/ folder for the reference files
3. Checking what code already exists (if any)
4. Starting on the highest-priority incomplete task from CLAUDE.md

If this is a continuing session, pick up where we left off. Check git log for recent work.

Current phase: Phase 1 PoC (testnet). Solo builder. Working testnet wallet with automated refresh.

Key constraint: Read SDK source before writing integration code. `git clone` first, understand the abstractions, then build.
